# Go Telegram Bot Template

Шаблон для создания Telegram ботов на языке Go с использованием принципов Clean Architecture.

Этот проект служит отправной точкой для создания масштабируемых и тестируемых ботов. Он включает в себя настроенную структуру папок, логгирование и Graceful Shutdown.

## 🛠 Технологический стек

*   **Язык:** Go 1.25+
*   **Архитектура:** Clean Architecture (Domain, Service, Repository)
*   **Логгирование:** `log/slog` (структурное логгирование)
*   **Конфигурация:** Переменные окружения
*   **База данных:** PostgreSQL/MongoDB

## 📂 Структура проекта

*   `cmd/bot/` — Точка входа в приложение (`main.go`).
*   `internal/config/` — Загрузка конфигурации из ENV.
*   `internal/domain/` — Доменные сущности и интерфейсы.
*   `internal/service/` — Бизнес-логика.
*   `internal/repository/` — Работа с данными (БД).

## ⚙️ Конфигурация

Приложение настраивается через переменные окружения (Environment Variables).

| Переменная | Описание | Значение по умолчанию | Обязательно |
| :--- | :--- | :--- | :--- |
| `BOT_TOKEN` | Токен вашего Telegram бота | - | **Да** |
| `LOG_LEVEL` | Уровень логгирования (`debug`, `info`, `warn`, `error`) | `info` | Нет |
| `DATABASE_URL` | Строка подключения к БД | - | Нет |

### Настройка в VS Code

В репозитории уже есть настроенный файл запуска `.vscode/launch.json`. Для начала работы:

1. Скопируйте файл `example.env` и переименуйте копию в `.env.dev`.
2. Заполните `.env.dev` необходимыми значениями (например, добавьте `BOT_TOKEN`).
3. Запустите отладку в VS Code (F5), выбрав конфигурацию **Launch Package**.

VS Code автоматически подхватит переменные окружения из файла `.env.dev`.

## 🚀 Запуск

1.  Клонируйте репозиторий:
    ```bash
    git clone https://github.com/viktor-kurbatov/tg_bot_template.git
    ```

2.  Установите переменные окружения и запустите бота:
    ```bash
    # Пример для Linux/macOS
    export BOT_TOKEN="your_token"
    go run cmd/bot/main.go
    ```

## 📝 Планы

*   [x] Добавить конфигурацию (env)
*   [ ] Подключить Telegram API
*   [ ] Реализовать работу с PostgreSQL
*   [ ] Добавить Docker Compose для быстрого старта

---
*Created by Viktor Kurbatov*
